# SNES Assembly Parser

A library for parsing SNES assembly code.

## Development

This project uses [uv](https://docs.astral.sh/uv/). Sync the environment
(including the dev dependency group) and run the linters and tests:

```console
$ uv sync
$ ./checks
```

## Usage

```console
$ uv run snes-assembly-parser --help
```
